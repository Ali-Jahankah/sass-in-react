import React from "react";
import { render } from "react-dom";

import App from "./App";

render(<App />, document.getElementById("root"));
// ===================================================================
// VS Code Bug for CSS Modules: Error: ENV variables=>>>> Adding a line of code for eslint in VS Code Settings for eslint ENV
